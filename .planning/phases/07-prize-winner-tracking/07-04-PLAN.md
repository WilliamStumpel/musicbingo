---
phase: 07-prize-winner-tracking
plan: 04
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - musicbingo_api/src/musicbingo_api/game_service.py
  - musicbingo_api/src/musicbingo_api/main.py
  - musicbingo_api/src/musicbingo_api/schemas.py
  - musicbingo_api/tests/test_card_registration.py
autonomous: true
---

<objective>
Add card recall functionality - ability to unregister individual cards or clear all registrations.

Purpose: Allow DJ to redistribute cards to new players between games without reloading the entire game.
Output: Two new API endpoints with tests.
</objective>

<context>
User request: "is there a way to recall all cards?"

Current state:
- Cards can be registered to player names
- Round reset clears played songs but keeps registrations
- No way to unregister cards

Requirement: Card recall must be SEPARATE from round reset.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GameService methods</name>
  <files>musicbingo_api/src/musicbingo_api/game_service.py</files>
  <action>
Add two methods to GameService:
- unregister_card(game_id, card_id) -> bool: Remove single registration
- clear_all_registrations(game_id) -> int: Clear all, return count
  </action>
  <done>Added both methods</done>
</task>

<task type="auto">
  <name>Task 2: Add API endpoints</name>
  <files>musicbingo_api/src/musicbingo_api/main.py, musicbingo_api/src/musicbingo_api/schemas.py</files>
  <action>
Add endpoints:
- DELETE /api/game/{game_id}/register-card/{card_id} - unregister single card
- POST /api/game/{game_id}/clear-registrations - clear all registrations

Add response schemas:
- UnregisterCardResponse
- ClearRegistrationsResponse
  </action>
  <done>Added both endpoints and schemas</done>
</task>

<task type="auto">
  <name>Task 3: Add tests</name>
  <files>musicbingo_api/tests/test_card_registration.py</files>
  <action>
Add tests:
- test_unregister_card
- test_unregister_card_not_registered
- test_unregister_card_game_not_found
- test_clear_registrations
- test_clear_registrations_empty
- test_clear_registrations_game_not_found
- test_clear_registrations_does_not_affect_round
- test_reset_round_does_not_clear_registrations
  </action>
  <done>Added 8 tests, all passing</done>
</task>

</tasks>

<verification>
- [x] DELETE endpoint unregisters single card
- [x] POST endpoint clears all registrations
- [x] Round reset does NOT clear registrations
- [x] Clear registrations does NOT affect played songs
- [x] All 24 tests pass
</verification>

<success_criteria>
- Both endpoints working
- Separate from round reset functionality
- Full test coverage
</success_criteria>
