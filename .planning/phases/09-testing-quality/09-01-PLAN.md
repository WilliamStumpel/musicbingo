---
phase: 09-testing-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicbingo_api/tests/test_prep_routes.py
  - musicbingo_api/tests/test_card_registration.py
  - musicbingo_api/tests/test_card_generation.py
autonomous: true
---

<objective>
Add unit tests for API endpoints added in Phases 7-8 (prep routes, card registration, winner detection, card generation).

Purpose: Ensure the newer API functionality has test coverage matching the original verification endpoints.
Output: Three new test files covering prep API, card registration, and card generation endpoints.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

# Existing test patterns to follow:
@musicbingo_api/tests/test_api.py
@musicbingo_api/tests/test_models.py

# Source files being tested:
@musicbingo_api/src/musicbingo_api/prep_routes.py
@musicbingo_api/src/musicbingo_api/game_routes.py
@musicbingo_api/src/musicbingo_api/card_generation_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for prep routes (venues, venue nights)</name>
  <files>musicbingo_api/tests/test_prep_routes.py</files>
  <action>
Create test file for prep API routes. Follow existing test_api.py patterns using TestClient.

Test cases for venues:
- GET /api/prep/venues returns empty list initially
- POST /api/prep/venues creates venue with name, returns ID
- GET /api/prep/venues/{id} returns venue details
- PUT /api/prep/venues/{id} updates venue name
- DELETE /api/prep/venues/{id} removes venue

Test cases for venue nights:
- POST /api/prep/venue-nights creates night with date, venue_id
- GET /api/prep/venue-nights returns list
- GET /api/prep/venue-nights?venue_id=X filters by venue
- PUT /api/prep/venue-nights/{id}/status updates status (draft/ready/completed)

Use pytest fixtures for test data setup. Clean up test database after tests.
  </action>
  <verify>pytest musicbingo_api/tests/test_prep_routes.py -v passes all tests</verify>
  <done>At least 10 test cases for prep routes, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for card registration and winner detection</name>
  <files>musicbingo_api/tests/test_card_registration.py</files>
  <action>
Create test file for card registration and winner detection endpoints.

Test cases for registration:
- POST /api/game/{id}/register-card registers card with player name
- GET /api/game/{id}/card-statuses returns registered cards
- Registration returns card_id, card_number, player_name, registered_at

Test cases for winner detection:
- Proactive detection triggers when song marked that completes pattern
- detected_winners list populated with card_id, player_name, pattern, detected_at
- Round reset clears detected_winners but keeps current_prize

Test cases for prizes:
- POST /api/game/{id}/prize sets current prize
- Prize persists across round reset

Use existing test fixtures (game creation, card bulk add) from test_api.py.
  </action>
  <verify>pytest musicbingo_api/tests/test_card_registration.py -v passes all tests</verify>
  <done>At least 8 test cases for registration and winner detection, all passing</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for card generation service</name>
  <files>musicbingo_api/tests/test_card_generation.py</files>
  <action>
Create test file for card generation API endpoints.

Test cases:
- POST /api/prep/games/{id}/generate creates cards and PDF
- Generation requires at least 48 songs in playlist
- Generation returns success with card_count
- GET /api/prep/games/{id}/download/pdf returns PDF file
- GET /api/prep/games/{id}/download/json returns JSON file
- Download endpoints return 404 if not yet generated

Mock the actual card generation (CardGenerator) to avoid slow PDF creation in tests.
Use unittest.mock.patch to mock the card_generation_service functions.
  </action>
  <verify>pytest musicbingo_api/tests/test_card_generation.py -v passes all tests</verify>
  <done>At least 6 test cases for card generation endpoints, all passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest musicbingo_api/tests/ -v passes all tests (existing + new)
- [ ] No import errors or missing dependencies
- [ ] New tests follow existing patterns in test_api.py
</verification>

<success_criteria>

- All tasks completed
- 24+ new test cases added across 3 files
- All tests pass
- Test coverage for Phases 7-8 API functionality
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-quality/09-01-SUMMARY.md`
</output>
