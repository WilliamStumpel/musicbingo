---
phase: 09-testing-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - musicbingo_verify/src/services/qrParser.test.js
  - musicbingo_verify/src/services/apiClient.test.js
  - musicbingo_verify/src/components/Scanner.test.jsx
  - musicbingo_verify/src/components/ResultDisplay.test.jsx
  - musicbingo_verify/src/App.test.js
autonomous: true
---

<objective>
Add unit tests for Scanner PWA services (qrParser, apiClient) and key components (Scanner, ResultDisplay).

Purpose: Test the QR parsing logic, API client error handling, and critical UI components that were untested.
Output: Service tests with mocked fetch, component tests with Testing Library.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

# Existing test utilities:
@musicbingo_verify/src/test-utils.js

# Source files being tested:
@musicbingo_verify/src/services/qrParser.js
@musicbingo_verify/src/services/apiClient.js
@musicbingo_verify/src/components/Scanner.jsx
@musicbingo_verify/src/components/ResultDisplay.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for qrParser service</name>
  <files>musicbingo_verify/src/services/qrParser.test.js</files>
  <action>
Create test file for QR parser service.

Test cases for parseQRData:
- Valid QR string with 3 pipe-separated parts returns {cardId, gameId, checksum}
- Empty string throws "Invalid QR code: empty or non-string value"
- Non-string input throws error
- Wrong number of parts throws "expected 3 parts"
- Invalid cardId UUID throws "card ID is not a valid UUID"
- Invalid gameId UUID throws "game ID is not a valid UUID"
- Checksum not 16 chars throws "checksum must be 16 characters"

Test cases for validateChecksum:
- Valid 16-char hex returns true
- Empty string returns false
- Wrong length returns false
- Non-hex characters returns false

Use mockUUID and mockGameId from test-utils.js for consistent test data.
  </action>
  <verify>npm test -- --watchAll=false --testPathPattern=qrParser.test.js passes</verify>
  <done>At least 10 test cases for qrParser, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for apiClient service</name>
  <files>musicbingo_verify/src/services/apiClient.test.js</files>
  <action>
Create test file for API client service.

Test cases for ApiClient.verifyCard:
- Returns verification result on 200 response
- Throws "Card or game not found" on 404
- Throws "Server error" on 500
- Throws with status code on other errors
- Handles network errors with helpful message

Test cases for ApiClient.healthCheck:
- Returns true on 200 response
- Returns false on non-200 response
- Returns false on network error

Test cases for registerCard:
- Returns registration result on success
- Throws error message from response on failure

Mock global fetch using jest.fn(). Use mockFetchResponse from test-utils.js.
  </action>
  <verify>npm test -- --watchAll=false --testPathPattern=apiClient.test.js passes</verify>
  <done>At least 8 test cases for apiClient, all passing</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for Scanner and ResultDisplay components</name>
  <files>musicbingo_verify/src/components/Scanner.test.jsx, musicbingo_verify/src/components/ResultDisplay.test.jsx, musicbingo_verify/src/App.test.js</files>
  <action>
Create/update component tests using React Testing Library.

Scanner.test.jsx:
- Renders scanner container
- Calls onScan callback when QR detected (mock QrScanner)
- Displays error state when scanner fails to start
- Shows processing indicator during scan

ResultDisplay.test.jsx:
- Shows winner UI (green background) when result.winner is true
- Shows non-winner UI (red background) when result.winner is false
- Displays card number from result
- Displays pattern name when winner
- Shows player name if registered
- Calls onReset when reset button clicked

Update App.test.js:
- Replace broken "learn react" test with actual app test
- Test renders app title
- Test tab navigation between Scan/Checklist

Mock qr-scanner library to avoid camera access in tests.
  </action>
  <verify>npm test -- --watchAll=false passes all scanner tests</verify>
  <done>At least 12 test cases across component files, all passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- --watchAll=false in musicbingo_verify passes all tests
- [ ] No console errors or warnings in test output
- [ ] Test coverage improved (check with --coverage)
</verification>

<success_criteria>

- All tasks completed
- 30+ new test cases for scanner PWA
- All tests pass including the fixed App.test.js
- Services and key components have test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-quality/09-02-SUMMARY.md`
</output>
