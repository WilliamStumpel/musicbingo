---
phase: 08-game-prep-ui
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - musicbingo_api/src/musicbingo_api/venue_repository.py
  - musicbingo_api/src/musicbingo_api/venue_routes.py
  - musicbingo_api/src/musicbingo_api/main.py
  - musicbingo_host/src/App.js
  - musicbingo_host/src/pages/PrepView.jsx
  - musicbingo_host/src/pages/PrepView.css
  - musicbingo_host/src/components/VenueManager.jsx
  - musicbingo_host/src/components/VenueManager.css
  - musicbingo_host/src/services/prepApi.js
autonomous: true
---

<objective>
Implement Venue CRUD operations with API endpoints and host app UI.

Purpose: Allow DJs to create and manage venues (bars, clubs) with branding info that can be reused across multiple game nights.
Output: Venue API endpoints, repository layer, and VenueManager React component in new Prep tab.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-game-prep-ui/08-01-SUMMARY.md

# Existing patterns
@musicbingo_api/src/musicbingo_api/schemas.py
@musicbingo_api/src/musicbingo_api/game_service.py
@musicbingo_host/src/pages/HostView.jsx
@musicbingo_host/src/services/gameApi.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Venue repository and API routes</name>
  <files>musicbingo_api/src/musicbingo_api/venue_repository.py, musicbingo_api/src/musicbingo_api/venue_routes.py, musicbingo_api/src/musicbingo_api/main.py</files>
  <action>
Create venue_repository.py with functions:
- list_venues() -> list[Venue]
- get_venue(id: int) -> Venue | None
- create_venue(name: str, logo_path: str | None, contact_info: str | None) -> Venue
- update_venue(id: int, name: str, logo_path: str | None, contact_info: str | None) -> Venue
- delete_venue(id: int) -> bool

Create venue_routes.py with FastAPI router:
- GET /api/prep/venues - List all venues
- GET /api/prep/venues/{id} - Get single venue
- POST /api/prep/venues - Create venue (accepts JSON body with name, contact_info)
- PUT /api/prep/venues/{id} - Update venue
- DELETE /api/prep/venues/{id} - Delete venue (cascades to nights/games)
- POST /api/prep/venues/{id}/logo - Upload logo file (multipart/form-data)

In main.py:
- Import and include venue_routes router with prefix="/api/prep"

Use Pydantic schemas for request/response validation. Follow existing patterns in schemas.py.
Logo upload saves to musicbingo_api/data/logos/{venue_id}_{filename} and stores path in database.
  </action>
  <verify>curl -s http://localhost:8000/api/prep/venues | python -m json.tool</verify>
  <done>All venue CRUD endpoints work, logo upload saves file and updates database</done>
</task>

<task type="auto">
  <name>Task 2: Create Prep page route and shell</name>
  <files>musicbingo_host/src/App.js, musicbingo_host/src/pages/PrepView.jsx, musicbingo_host/src/pages/PrepView.css, musicbingo_host/src/services/prepApi.js</files>
  <action>
Update App.js to add /prep route:
- Add import for PrepView
- Add Route path="/prep" element={<PrepView />}

Create prepApi.js service with functions:
- getVenues() - GET /api/prep/venues
- createVenue(data) - POST /api/prep/venues
- updateVenue(id, data) - PUT /api/prep/venues/{id}
- deleteVenue(id) - DELETE /api/prep/venues/{id}
- uploadVenueLogo(id, file) - POST /api/prep/venues/{id}/logo (FormData)

Create PrepView.jsx page shell:
- Header with "Game Prep" title and navigation tabs (Venues, Nights, Games)
- Currently selected tab state (default: Venues)
- Tab content area that renders appropriate component based on selected tab
- For now, only Venues tab is functional (others show "Coming soon")
- Link back to Host View in header

Create PrepView.css:
- Full-page layout similar to HostView
- Tab navigation styling
- Responsive for laptop screens (primary target for prep work)

Use same API base URL detection pattern as gameApi.js (check for localhost vs production).
  </action>
  <verify>npm start in musicbingo_host, navigate to /prep, verify page loads with tabs</verify>
  <done>Prep page accessible at /prep with working tab navigation</done>
</task>

<task type="auto">
  <name>Task 3: Create VenueManager component</name>
  <files>musicbingo_host/src/components/VenueManager.jsx, musicbingo_host/src/components/VenueManager.css</files>
  <action>
Create VenueManager.jsx:
- Fetch and display list of venues on mount
- Add Venue button opens inline form (not modal)
- Venue form fields: Name (required), Contact Info (optional textarea)
- Edit button on each venue row opens form with prefilled values
- Delete button with window.confirm() confirmation
- Logo upload: File input, preview of current logo, upload on selection
- Display logo thumbnail (40x40) in venue list if present
- Loading states and error handling

Create VenueManager.css:
- Card-based venue list layout
- Inline form styling consistent with host app
- Logo thumbnail styling
- Action button styling (Edit, Delete)

Follow patterns from existing components (SongChecklist for list styling, CardStatusPanel for panels).
  </action>
  <verify>Create a venue, edit it, upload logo, delete it - all operations work</verify>
  <done>VenueManager displays venues, supports full CRUD operations including logo upload</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All venue API endpoints respond correctly
- [ ] Logo upload saves file and returns updated venue
- [ ] /prep route loads PrepView page
- [ ] VenueManager shows venues list
- [ ] Can create, edit, delete venues from UI
- [ ] Logo displays in venue list
- [ ] No console errors
</verification>

<success_criteria>

- All tasks completed
- Full venue CRUD workflow functional
- UI matches existing host app styling
- Logo files persist across server restarts
</success_criteria>

<output>
After completion, create `.planning/phases/08-game-prep-ui/08-02-SUMMARY.md`
</output>
