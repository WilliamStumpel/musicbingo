---
phase: 08-game-prep-ui
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - musicbingo_api/src/musicbingo_api/venue_night_repository.py
  - musicbingo_api/src/musicbingo_api/venue_night_routes.py
  - musicbingo_api/src/musicbingo_api/main.py
  - musicbingo_host/src/services/prepApi.js
  - musicbingo_host/src/pages/PrepView.jsx
  - musicbingo_host/src/components/VenueNightManager.jsx
  - musicbingo_host/src/components/VenueNightManager.css
autonomous: true
---

<objective>
Implement VenueNight CRUD operations with API endpoints and host app UI.

Purpose: Allow DJs to organize game nights by date and venue, tracking which nights are prepared, ready, or completed.
Output: VenueNight API endpoints, repository layer, and VenueNightManager React component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-game-prep-ui/08-01-SUMMARY.md
@.planning/phases/08-game-prep-ui/08-02-SUMMARY.md

# Existing patterns
@musicbingo_api/src/musicbingo_api/venue_repository.py
@musicbingo_api/src/musicbingo_api/venue_routes.py
@musicbingo_host/src/components/VenueManager.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VenueNight repository and API routes</name>
  <files>musicbingo_api/src/musicbingo_api/venue_night_repository.py, musicbingo_api/src/musicbingo_api/venue_night_routes.py, musicbingo_api/src/musicbingo_api/main.py</files>
  <action>
Create venue_night_repository.py with functions:
- list_venue_nights(venue_id: int | None = None) -> list[VenueNight] (with venue info joined)
- get_venue_night(id: int) -> VenueNight | None (with venue info and game count)
- create_venue_night(venue_id: int, date: str, notes: str | None) -> VenueNight
- update_venue_night(id: int, venue_id: int, date: str, status: str, notes: str | None) -> VenueNight
- delete_venue_night(id: int) -> bool

Create venue_night_routes.py with FastAPI router:
- GET /api/prep/nights - List all nights (query param: venue_id for filtering)
- GET /api/prep/nights/{id} - Get single night with game count
- POST /api/prep/nights - Create night
- PUT /api/prep/nights/{id} - Update night (including status)
- DELETE /api/prep/nights/{id} - Delete night (cascades to games)

In main.py:
- Import and include venue_night_routes router

Return venue name in list/get responses (JOIN with venues table).
Return game_count in responses (COUNT from games table).
Status values: draft, ready, completed.
  </action>
  <verify>curl -s http://localhost:8000/api/prep/nights | python -m json.tool</verify>
  <done>All venue night CRUD endpoints work, returns venue info and game count</done>
</task>

<task type="auto">
  <name>Task 2: Add VenueNight API functions to prepApi.js</name>
  <files>musicbingo_host/src/services/prepApi.js</files>
  <action>
Add to prepApi.js:
- getVenueNights(venueId = null) - GET /api/prep/nights with optional venue_id query param
- getVenueNight(id) - GET /api/prep/nights/{id}
- createVenueNight(data) - POST /api/prep/nights (data: {venue_id, date, notes})
- updateVenueNight(id, data) - PUT /api/prep/nights/{id}
- deleteVenueNight(id) - DELETE /api/prep/nights/{id}

Follow existing patterns in the file.
  </action>
  <verify>Import prepApi in browser console, call getVenueNights(), verify response</verify>
  <done>All VenueNight API functions added and working</done>
</task>

<task type="auto">
  <name>Task 3: Create VenueNightManager component</name>
  <files>musicbingo_host/src/components/VenueNightManager.jsx, musicbingo_host/src/components/VenueNightManager.css, musicbingo_host/src/pages/PrepView.jsx</files>
  <action>
Create VenueNightManager.jsx:
- Fetch and display list of venue nights on mount (sorted by date, newest first)
- Status badge for each night (draft=gray, ready=green, completed=blue)
- Venue name and game count displayed in list
- Add Night button opens inline form
- Form fields: Venue (dropdown from venues list), Date (date picker), Notes (optional textarea)
- Edit button opens form with prefilled values
- Status dropdown in edit mode (draft/ready/completed)
- Delete button with window.confirm() confirmation
- Filter dropdown to show all or by venue
- Loading states and error handling

Create VenueNightManager.css:
- Card-based night list with date prominently displayed
- Status badge styling (color-coded)
- Date picker and dropdown styling
- Action button styling

Update PrepView.jsx:
- Render VenueNightManager when Nights tab selected
- Pass any needed props

Follow component patterns from VenueManager.
  </action>
  <verify>Create a venue night, edit status to ready, filter by venue, delete night</verify>
  <done>VenueNightManager displays nights, supports full CRUD with venue selection and status</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All venue night API endpoints respond correctly
- [ ] Nights tab in PrepView loads VenueNightManager
- [ ] Can create night with venue selection and date
- [ ] Status badges display correctly
- [ ] Filter by venue works
- [ ] Edit and delete operations work
- [ ] Game count displays (0 for new nights)
- [ ] No console errors
</verification>

<success_criteria>

- All tasks completed
- Full venue night CRUD workflow functional
- Venue dropdown populated from database
- Status transitions work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/08-game-prep-ui/08-03-SUMMARY.md`
</output>
