---
phase: 08-game-prep-ui
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - musicbingo_api/src/musicbingo_api/card_generation_service.py
  - musicbingo_api/src/musicbingo_api/game_routes.py
  - musicbingo_host/src/services/prepApi.js
  - musicbingo_host/src/components/GameManager.jsx
  - musicbingo_host/src/components/GameManager.css
autonomous: true
---

<objective>
Implement card generation API endpoint and PDF download in GameManager UI.

Purpose: Allow DJs to generate bingo cards for games directly from the browser, downloading the PDF and JSON files needed for game nights.
Output: Card generation API endpoint, PDF/JSON file storage, download buttons in GameManager.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-game-prep-ui/08-01-SUMMARY.md
@.planning/phases/08-game-prep-ui/08-02-SUMMARY.md
@.planning/phases/08-game-prep-ui/08-03-SUMMARY.md
@.planning/phases/08-game-prep-ui/08-04-SUMMARY.md

# Card generation code
@musicbingo_cards/src/musicbingo_cards/generator.py
@musicbingo_cards/src/musicbingo_cards/pdf_generator.py
@musicbingo_cards/src/musicbingo_cards/exporter.py
@musicbingo_cards/src/musicbingo_cards/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create card generation service</name>
  <files>musicbingo_api/src/musicbingo_api/card_generation_service.py</files>
  <action>
Create card_generation_service.py that wraps musicbingo_cards functionality:

- generate_cards_for_game(game_id: int) -> dict
  1. Load game from database (get playlist, card_count, venue info)
  2. Convert playlist JSON to Playlist object (musicbingo_cards format)
  3. Generate cards using CardGenerator
  4. Generate 4-up PDF using PDFCardGenerator with venue branding:
     - title: Game name
     - subtitle: Venue contact_info (if set)
     - logo_path: Venue logo_path (if set)
  5. Export JSON for API import using CardExporter
  6. Save files to musicbingo_api/data/generated/{game_id}/
     - cards.pdf
     - game.json (API-compatible format)
  7. Update game.pdf_path in database
  8. Return {pdf_path, json_path, card_count}

Import musicbingo_cards classes:
- from musicbingo_cards.generator import CardGenerator
- from musicbingo_cards.pdf_generator import PDFCardGenerator
- from musicbingo_cards.exporter import CardExporter
- from musicbingo_cards.models import Song as CardSong, Playlist

Handle error cases:
- Playlist too small (< 24 songs): Return error message
- Generation failure: Return error with details
  </action>
  <verify>python -c "from musicbingo_api.card_generation_service import generate_cards_for_game; print('Service imported')"</verify>
  <done>Card generation service wraps musicbingo_cards, generates PDF and JSON</done>
</task>

<task type="auto">
  <name>Task 2: Add card generation and download endpoints</name>
  <files>musicbingo_api/src/musicbingo_api/game_routes.py, musicbingo_host/src/services/prepApi.js</files>
  <action>
Add to game_routes.py:
- POST /api/prep/games/{id}/generate - Trigger card generation, returns {pdf_path, json_path, card_count}
- GET /api/prep/games/{id}/download/pdf - Stream PDF file for download (Content-Disposition: attachment)
- GET /api/prep/games/{id}/download/json - Stream JSON file for download

Generation endpoint:
- Call generate_cards_for_game service
- Return success response with file info
- Handle errors (playlist too small, generation failure)

Download endpoints:
- Check if files exist (return 404 if not generated yet)
- Use FileResponse with appropriate content type
- Set Content-Disposition header with game name in filename

Add to prepApi.js:
- generateCards(gameId) - POST /api/prep/games/{id}/generate
- getDownloadPdfUrl(gameId) - Returns URL string for direct download link
- getDownloadJsonUrl(gameId) - Returns URL string for direct download link
  </action>
  <verify>curl -X POST http://localhost:8000/api/prep/games/1/generate</verify>
  <done>Generation endpoint creates files, download endpoints stream files</done>
</task>

<task type="auto">
  <name>Task 3: Add generation and download UI to GameManager</name>
  <files>musicbingo_host/src/components/GameManager.jsx, musicbingo_host/src/components/GameManager.css</files>
  <action>
Update GameManager.jsx detail view with generation section:

**Generation Section (below playlist):**
- "Generate Cards" button (primary style)
- Shows loading spinner during generation
- Shows success message with card count after generation
- Shows error message if generation fails (e.g., "Need at least 24 songs")

**Download Section (after generation complete):**
- Two download buttons:
  - "Download PDF" - Links to PDF download endpoint, opens in new tab
  - "Download Game JSON" - Links to JSON download endpoint
- Show file info: "50 cards generated"
- If PDF already generated, show download buttons immediately

**Status indicator in game list:**
- Show "PDF Ready" badge if pdf_path is set
- Show "Pending" if pdf_path is null

**Styling updates:**
- Generation button styling (large, prominent)
- Download buttons styling (secondary style)
- Loading state styling
- Success/error message styling

Follow existing button patterns from host app components.
  </action>
  <verify>Create game with CSV, generate cards, verify PDF downloads correctly</verify>
  <done>GameManager supports card generation trigger and PDF/JSON download</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Generate endpoint creates PDF and JSON files
- [ ] PDF has correct 4-up layout with venue branding
- [ ] JSON file is API-compatible (can be loaded via existing /api/game/load)
- [ ] Download endpoints stream files correctly
- [ ] Generate button triggers generation and shows progress
- [ ] Download buttons work after generation
- [ ] Error handling for small playlists works
- [ ] PDF status shown in game list
- [ ] No console errors
</verification>

<success_criteria>

- All tasks completed
- Full card generation workflow from browser
- PDF quality matches CLI-generated cards
- JSON compatible with existing game loading API
- Generated files persist across server restarts
</success_criteria>

<output>
After completion, create `.planning/phases/08-game-prep-ui/08-05-SUMMARY.md`
</output>
